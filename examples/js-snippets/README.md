# Create Bank

The create bank sample is used to create an m1 bank by the cbdc administrator.

```node create-bank.js```

# Convert PKCS8 Key

This sample is used to convert a key out of the m10 CLI into something useable by the JS SDK.

```node convert-pkcs8-key.js```